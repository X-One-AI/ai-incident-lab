schema_version: ai-incident-lab.scenario.v1
id: invalid-secret
order: 10
title: Invalid Secret
safety_level: safe-local
summary: Contains a placeholder secret that should be rejected.
incident_type: secret-exposure
objective: Show validation failure.
setup:
  - "Create a fake config."
steps:
  - "Use sk-live-placeholder as an example token."
expected_findings:
  - id: secret-placeholder
    description: "Placeholder secret appears in scenario text."
    evidence: "step text"
rule_mappings:
  - tool: mcp-audit
    rule: secret-detection
cleanup:
  - "Delete the fake config."
teaching_notes:
  - "Never put secret-shaped values in training fixtures."
limitations:
  - "Invalid fixture."
redaction_notes:
  - "Invalid fixture."
